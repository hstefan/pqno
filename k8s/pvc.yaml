apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pqno-data
  namespace: pqno
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
